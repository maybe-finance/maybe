import type { CreateVehicleFields } from '@maybe-finance/client/shared'

export function AddStock({ defaultValues }: { defaultValues: Partial<CreateVehicleFields> }) {
    return <>{defaultValues}</>
}
