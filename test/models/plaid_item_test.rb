require "test_helper"

class PlaidItemTest < ActiveSupport::TestCase
  test "stores hashed access token" do
    # access_token = ''
    # plaid_item = PlaidItem.create!(family: families(:one), access_token: "1234567890")
    # assert_equal PlaidItem.find(plaid_item.id).access_token, plaid_item.access_token
    skip
  end
end
