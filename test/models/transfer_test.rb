require "test_helper"

class TransferTest < ActiveSupport::TestCase
  test "transfer cannot have more than 2 transactions" do
    flunk
  end

  test "transfer cannot have 2 transactions from the same account" do
    flunk
  end

  test "transfer with 2 transactions must have inflow and outflow" do
    flunk
  end

  test "transfer is valid with a single transaction" do
    flunk
  end

  test "transaction with existing transfer details cannot be assigned to new transfer" do
    flunk
  end
end
