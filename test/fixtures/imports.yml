completed_import:
  account: checking
  column_mappings:
  raw_csv: |
    col1,col2
    val1,val2
  created_at: <%= 2.days.ago %>

pending_import:
  account: checking
  column_mappings:
  created_at: <%= 20.minutes.ago %>

empty_import:
  account: checking
  column_mappings:
  created_at: <%= 1.minute.ago %>
