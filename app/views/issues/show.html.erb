<%= drawer do %>
  <article class="prose">
    <%= tag.h2 @issue.title %>

    <%= tag.h3 t(".overview") %>
    <%= render @issue.to_description_partial_path, issue: @issue %>

    <%= tag.h3 t(".action") %>
    <%= render @issue.to_action_partial_path, issue: @issue %>
  </article>
<% end %>

